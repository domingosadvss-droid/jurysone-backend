/**
 * JURYSONE — Configuração do Dashboard — TEMPLATE
 *
 * Copie para config.js e ajuste a URL do seu backend:
 *   cp config.example.js config.js
 *
 * Não são necessárias chaves de API aqui.
 * ZapSign, Asaas e DataJud são configurados nas variáveis de ambiente do servidor NestJS.
 */
window.JURYSONE_CONFIG = {
  // URL do backend NestJS
  // Desenvolvimento: http://localhost:3001
  // Produção:        https://api.jurysone.com.br
  API_URL: "http://localhost:3001",
};
